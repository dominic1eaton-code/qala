# Scala Execution Plane Services

## Projects

- `workflow-ci-cd-service` (`:8084`)
- `data-platform-service` (`:8086`)
- `notifications-service` (`:8089`)

## Run

```powershell
cd scala
sbt workflow/run
sbt dataPlatform/run
sbt notifications/run
```
