# web client prompts

read the documentation in C:\dev\ws\qala\qala-docs\sdd to get the structure of qala platform and read all of the pages in C:\dev\ws\qala\qala-docs\pages and convert and implement them into an angular+tailwindcss application in C:\dev\ws\qala\qala-client\web-client\prod-web-client where the login page links to both the registration and dashboard pages, and the registration page then links to the onboarding pages, which have multiple views as a user goes through the onboarding process, which then goes to the loading page, while the platform is being setup for the user, which then goes to the dashboard page.

Then set up all of the other pages, having a primary side navigation panel that only has the 8 most important systems for a user to navigate to, and putting all of the other pages+features+functionality as subpages/subcomponents in those 8 main components/pages. Also create subcomponents/subpages within subcomponents/subpages where it makes the most sense, and balance between component/page depth and breadth, flat vs deep page/component hierarchy structure. Be sure to make sure that all of the pages+features+functionality+structures of all of the pages in C:\dev\ws\qala\qala-docs\pages are implemented and navigatable in the app, all accessible from the 8 main components/pages and their subcomponents/subpages.

also make sure that the entire platform has a style that is consistent with the login.html / registration.html / onboarding pages / dashboard.html pages, where all of the other pages are refactored to match the consistent style of these pages, making sure that there is one uniform style of the entire platform

---

read the documentation in C:\dev\ws\qala\qala-docs\sdd to get the structure of qala platform and read all of the pages in C:\dev\ws\qala\qala-docs\pages and convert and implement them into an angular+tailwindcss application in C:\dev\ws\qala\qala-client\web-client\prod-web-client where the login page links to both the registration and dashboard pages, and the registration page then links to the onboarding pages, which have multiple views as a user goes through the onboarding process, which then goes to the loading page, while the platform is being setup for the user, which then goes to the dashboard page.

Then set up all of the other pages, having a primary side navigation panel that only has the 8 most important systems for a user to navigate to, and putting all of the other pages+features+functionality as subpages/subcomponents in those 8 main components/pages. Also create subcomponents/subpages within subcomponents/subpages where it makes the most sense, and balance between component/page depth and breadth, flat vs deep page/component hierarchy structure. Be sure to make sure that all of the pages+features+functionality+structures of all of the pages in C:\dev\ws\qala\qala-docs\pages are implemented and navigatable in the app, all accessible from the 8 main components/pages and their subcomponents/subpages.

also make sure that the entire platform has a style that is consistent with the login.html / registration.html / onboarding pages / dashboard.html pages, where all of the other pages are refactored to match the consistent style of these pages, making sure that there is one uniform style of the entire platform

---

communication relay process
    - if message sent
    - communicate message received
    - communicate progress
    - handoff

---

make all of the pages in C:\dev\ws\qala\qala-client\web-client\prod-dev-client scrollable and have the scrollbars match the aesthetics of the rest of the platform. also make the logged in pages have darker background colors and more vibrant accent colors, for sharper color contrasts on the users logged in pages

---

solution workbench
benchmarking
QA, quality management

---